enum Status { initial, loading, error, completed }

enum PostStatus { pending, approved, active, completed, rejected }

enum ApplicationStatus { all, applied, ongoing, completed }

enum WorkMode { online, offline }

enum PaymentStatus { success, failed }

enum Amount { debit, credit }

enum PaymentMethod { bank, upi, phone }

enum InstagramStatus { pending , approved, rejected , initial}
